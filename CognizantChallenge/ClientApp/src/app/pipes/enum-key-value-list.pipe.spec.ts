import { EnumKeyValueListPipe } from './enum-key-value-list.pipe';

describe('EnumKeyValueListPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumKeyValueListPipe();
    expect(pipe).toBeTruthy();
  });
});
